import React from "react";
import { Typography, Box } from "@material-ui/core";

const SignUpHeader = () => (
  <Box textAlign="center">
    <Typography variant="h3">Create your free account</Typography>
  </Box>
);

export default SignUpHeader;
